upstream backend {
    server 127.0.0.1:4000;
}

upstream web {
    server 127.0.0.1:3000;
}

upstream seller {
    server 127.0.0.1:3003;
}

upstream admin {
    server 127.0.0.1:3002;
}

# Rate limiting
limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;
limit_req_zone $binary_remote_addr zone=auth:10m rate=5r/s;

# ─── API Backend (api.xelnova.in) ───
server {
    listen 80;
    server_name api.xelnova.in;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name api.xelnova.in;

    ssl_certificate /etc/letsencrypt/live/api.xelnova.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.xelnova.in/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    client_max_body_size 10M;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location /api/v1/auth/ {
        limit_req zone=auth burst=10 nodelay;
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ─── Web App (xelnova.in) ───
server {
    listen 80;
    server_name xelnova.in www.xelnova.in;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name xelnova.in www.xelnova.in;

    ssl_certificate /etc/letsencrypt/live/xelnova.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/xelnova.in/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://web;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ─── Seller Dashboard (seller.xelnova.in) ───
server {
    listen 80;
    server_name seller.xelnova.in;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name seller.xelnova.in;

    ssl_certificate /etc/letsencrypt/live/seller.xelnova.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/seller.xelnova.in/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://seller;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ─── Admin Dashboard (admin.xelnova.in) ───
server {
    listen 80;
    server_name admin.xelnova.in;
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl http2;
    server_name admin.xelnova.in;

    ssl_certificate /etc/letsencrypt/live/admin.xelnova.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin.xelnova.in/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://admin;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
