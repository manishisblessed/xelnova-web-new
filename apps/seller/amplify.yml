version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci
    build:
      commands:
        - npx turbo run build --filter=@xelnova/seller
  artifacts:
    baseDirectory: apps/seller/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/seller/.next/cache/**/*
